# Core
streamlit>=1.35.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Document Processing
pymupdf>=1.23.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
markdown>=3.5.0
openpyxl>=3.1.0
pandas>=2.1.0
tabulate>=0.9.0  # for pandas .to_markdown()

# Embeddings & Vector DB
sentence-transformers>=2.7.0
faiss-cpu>=1.7.4  # Use faiss-gpu if CUDA available

# LLM (Transformers for local models)
transformers>=4.40.0
accelerate>=0.27.0
huggingface-hub>=0.22.0
torch>=2.1.0  # CPU version; install GPU version separately if needed

# Export
reportlab>=4.0.0  # PDF fallback (lightweight)
# weasyprint>=60.0  # Optional: better PDF rendering (install separately if needed)

# Utilities
watchdog>=4.0.0   # folder watching
requests>=2.31.0
numpy>=1.26.0
